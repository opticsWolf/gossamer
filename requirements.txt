ddgs>=2.0
httpx>=0.27
pdf_oxide>=0.1
office-oxide @ git+https://github.com/yfedoseev/office_oxide.git
browser-oxide>=0.1
# meta-oxide: Rust-backed HTML metadata extractor (OG, Twitter, JSON-LD, etc.)
# NOTE: Upstream has packaging bug — build locally from git or install pre-built wheel.
#   pip install git+https://github.com/yfedoseev/meta_oxide.git  # may need manual __init__.py fix
# meta-oxide>=0.1
pydantic>=2.7.0
tiktoken>=0.5.0
pytest>=7.0
pytest-asyncio>=0.21
